DATABASE_USER="YOUR_USERNAME"
DATABASE_PASSWORD="YOUR_PASSWORD"
DATABASE_NAME="url_shortener"
DATABASE_HOST="db"
DATABASE_PORT="5432"
# A full DATABASE_URL is provided for tools (Prisma CLI) that read it directly.
# Keep the individual variables too so docker-compose can construct the same value.
DATABASE_URL_example="postgresql://user:password@db:5432/database"
DATABASE_URL=postgresql://${DATABASE_USER}:${DATABASE_PASSWORD}@${DATABASE_HOST}:${DATABASE_PORT}/${DATABASE_NAME}

JWT_SECRET="your_jwt_secret"
BASE_URL="http://localhost:3000"